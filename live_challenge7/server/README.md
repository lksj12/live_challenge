# 서버

SQLite 데이터베이스, 인증, 세션 폐기, 노트 API와 관리자 API를
관리합니다.

## 현재 구현

- 버전 기반 SQLite 마이그레이션
- 사용자, 세션, 노트, 태그, 감사 로그 스키마
- `GET /api/health` 상태 확인
- Argon2id 기반 회원가입과 로그인
- HttpOnly 쿠키와 서버 폐기 방식의 세션
- 메모리 데이터베이스 기반 자동 테스트

로컬 데이터베이스는 `data/keeply.db`에 생성되며 Git에 포함되지 않습니다.
